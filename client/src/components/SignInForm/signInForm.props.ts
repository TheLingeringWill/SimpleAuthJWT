/**
 * Props for a sign-in form component.
 *
 * @typedef {Object} SignInFormProps
 * @property {string} email - The email address for signing in.
 * @property {string} password - The password for signing in.
 */
type SignInFormProps = {
  email: string;
  password: string;
};

export default SignInFormProps;
